{
  "_note": "The argument schema of each Google Calendar tool this repository calls, as returned by a READ-ONLY GET /api/v3.1/tools/{slug} on 2026-09-15 (all five at toolkit version 20260915_00). Recorded rather than remembered: tests/test_composio_contract.py asserts that every argument scripts/calendar_io.py sends is a real property of the tool it is sent to, which is a check that only means something against the provider's own schema. See README.md in this directory for how it was recorded and how to re-record it.",
  "tools": {
    "GOOGLECALENDAR_CREATE_EVENT": {
      "properties": {
        "attendees": {
          "description": "List of attendees. Each attendee can be either: (1) A string email address (e.g., 'user@example.com'), or (2) An object with 'email' (required), 'optional' (boo",
          "type": "array"
        },
        "birthdayProperties": {
          "description": "Properties for birthday events.",
          "type": "object"
        },
        "calendar_id": {
          "description": "Calendar identifier. Use 'primary' (recommended) for the user's main calendar. Alternatively, use a calendar ID from the user's accessible calendar list. Calend",
          "type": "string"
        },
        "create_meeting_room": {
          "description": "Defaults to True. When True, for CREATE operations creates a Google Meet link; for UPDATE operations preserves existing conference data if present, or adds a ne",
          "type": "boolean"
        },
        "description": {
          "description": "Description of the event. Can contain HTML. Optional. Must be omitted for 'birthday' event type.",
          "type": "string"
        },
        "end_datetime": {
          "description": "Event end time in ISO 8601 format: YYYY-MM-DDTHH:MM:SS. When provided, this parameter takes precedence over event_duration_hour and event_duration_minutes. If n",
          "type": "string"
        },
        "eventType": {
          "description": "Type of the event, immutable post-creation. 'workingLocation' (REQUIRES Google Workspace Enterprise). Note: 'fromGmail' events cannot be created via API.",
          "type": "string"
        },
        "event_duration_hour": {
          "description": "Number of hours for the event duration. Supports multi-day events (e.g., 240 hours = 10 days). For durations under 1 hour, use event_duration_minutes instead. I",
          "type": "integer"
        },
        "event_duration_minutes": {
          "description": "Duration in minutes (0-59 ONLY). NEVER use 60+ minutes - use event_duration_hour=1 instead. Maximum value is 59. Combined duration (hours + minutes) must be gre",
          "type": "integer"
        },
        "exclude_organizer": {
          "description": "If True, the organizer will NOT be added as an attendee. Default is False (organizer is included).",
          "type": "boolean"
        },
        "extended_properties": {
          "description": "Extended properties of the event for storing custom metadata. Contains 'private' (visible only on this calendar) and/or 'shared' (visible to all attendees) dict",
          "type": "object"
        },
        "focusTimeProperties": {
          "description": "Properties for focusTime events. REQUIRES Google Workspace Enterprise account with Focus Time feature enabled.",
          "type": "object"
        },
        "guestsCanInviteOthers": {
          "description": "Whether attendees other than the organizer can invite others to the event.",
          "type": "boolean"
        },
        "guestsCanModify": {
          "description": "If True, guests can modify the event.",
          "type": "boolean"
        },
        "guestsCanSeeOtherGuests": {
          "description": "Whether attendees other than the organizer can see who the event's attendees are.",
          "type": "boolean"
        },
        "location": {
          "description": "Geographic location of the event as free-form text.",
          "type": "string"
        },
        "outOfOfficeProperties": {
          "description": "Properties for outOfOffice events.",
          "type": "object"
        },
        "recurrence": {
          "description": "List of RRULE, EXRULE, RDATE, EXDATE lines for recurring events. Supported frequencies: DAILY, WEEKLY, MONTHLY, YEARLY. For recurring events, start.timeZone and",
          "type": "array"
        },
        "send_updates": {
          "description": "Options for who should receive notifications about event changes.",
          "type": "string"
        },
        "start_datetime": {
          "description": "REQUIRED. Event start time in ISO 8601 format: YYYY-MM-DDTHH:MM:SS. IMPORTANT: Natural language expressions like 'tomorrow', 'next Monday', '2pm tomorrow' are N",
          "type": "string"
        },
        "summary": {
          "description": "Summary (title) of the event.",
          "type": "string"
        },
        "timezone": {
          "description": "IANA timezone name from the timezone database (e.g., 'America/New_York', 'Europe/London', 'Asia/Jerusalem', 'UTC'). Required if datetime is naive. For recurring",
          "type": "string"
        },
        "transparency": {
          "description": "'opaque' (busy) or 'transparent' (available).",
          "type": "string"
        },
        "visibility": {
          "description": "Event visibility: 'default', 'public', 'private', or 'confidential'.",
          "type": "string"
        },
        "workingLocationProperties": {
          "description": "Properties for workingLocation events. REQUIRES Google Workspace Enterprise.  Constraints discovered from testing: - Must set transparency='transparent' and vis",
          "type": "object"
        }
      },
      "required": [
        "start_datetime"
      ],
      "toolkit": "googlecalendar",
      "version": "20260915_00"
    },
    "GOOGLECALENDAR_DELETE_EVENT": {
      "properties": {
        "calendar_id": {
          "description": "Identifier of the Google Calendar (e.g., email address, specific ID, or 'primary' for the authenticated user's main calendar) from which the event will be delet",
          "type": "string"
        },
        "event_id": {
          "description": "Unique identifier of the event to delete. For standalone events, use the base event ID (e.g., 'abc123def456'). For recurring event instances, use the instance I",
          "type": "string"
        },
        "send_notifications": {
          "description": "Deprecated. Whether to send notifications about the deletion of the event. Note that some emails might still be sent.",
          "type": "boolean"
        },
        "send_updates": {
          "description": "Options for who should receive notifications about event changes.",
          "type": "string"
        }
      },
      "required": [
        "event_id"
      ],
      "toolkit": "googlecalendar",
      "version": "20260915_00"
    },
    "GOOGLECALENDAR_EVENTS_GET": {
      "properties": {
        "calendar_id": {
          "description": "Identifier of the Google Calendar (e.g., email address, specific ID, or 'primary' for the authenticated user's main calendar) from which to retrieve the event.",
          "type": "string"
        },
        "event_id": {
          "description": "REQUIRED. The unique identifier of the specific event to retrieve. You must already know this ID (e.g., from a previous EVENTS_LIST call or event creation respo",
          "type": "string"
        },
        "max_attendees": {
          "description": "Maximum number of attendees to include in the response. If there are more than the specified number, only the participant is returned.",
          "type": "integer"
        },
        "time_zone": {
          "description": "Time zone used in the response. If not specified, the calendar's time zone is used.",
          "type": "string"
        }
      },
      "required": [
        "event_id"
      ],
      "toolkit": "googlecalendar",
      "version": "20260915_00"
    },
    "GOOGLECALENDAR_EVENTS_LIST": {
      "properties": {
        "alwaysIncludeEmail": {
          "description": "Deprecated and ignored.",
          "type": "boolean"
        },
        "calendarId": {
          "description": "Calendar identifier. Use \"primary\" for the user's main calendar, or a calendar ID from the user's accessible calendar list. Arbitrary email addresses will NOT w",
          "type": "string"
        },
        "eventTypes": {
          "description": "Event types to return. Google accepts this query parameter more than once, so provide a list to include multiple types. A single string is also accepted for com",
          "type": "array"
        },
        "fields": {
          "description": "Google partial-response selector that limits which fields are returned. Use comma-separated fields and nested selectors, for example 'items(id,status,summary,st",
          "type": "string"
        },
        "iCalUID": {
          "description": "Specifies an event ID in the iCalendar format to be provided in the response. Optional. Use this if you want to search for an event by its iCalendar ID.",
          "type": "string"
        },
        "maxAttendees": {
          "description": "The maximum number of attendees to include in the response. If there are more than the specified number of attendees, only the participant is returned. Optional",
          "type": "integer"
        },
        "maxResults": {
          "description": "Maximum number of events returned on one result page. The number of events in the resulting page may be less than this value, or none at all, even if there are ",
          "type": "integer"
        },
        "orderBy": {
          "description": "The order of the events returned in the result. Optional. The default is an unspecified, stable order. Acceptable values are: \"startTime\", \"updated\". When set t",
          "type": "string"
        },
        "pageToken": {
          "description": "Opaque pagination token from a previous response's nextPageToken field. Must be the exact string returned by the API - do not use placeholder values like 'NEXT'",
          "type": "string"
        },
        "privateExtendedProperty": {
          "description": "Extended properties constraint specified as propertyName=value. Matches only private properties. This parameter might be repeated multiple times to return event",
          "type": null
        },
        "query": {
          "description": "Free text search terms to find events that match these terms in various fields. Optional.",
          "type": "string"
        },
        "sharedExtendedProperty": {
          "description": "Extended properties constraint specified as propertyName=value. Matches only shared properties. This parameter might be repeated multiple times to return events",
          "type": null
        },
        "showDeleted": {
          "description": "Include cancelled events (status=\"cancelled\"). Optional; default is false. This surfaces cancelled (soft-deleted) events, not items in the Trash. When syncToken",
          "type": "boolean"
        },
        "showHiddenInvitations": {
          "description": "Whether to include hidden invitations in the result. Optional. The default is False. Hidden invitations are events where your attendee entry has responseStatus=",
          "type": "boolean"
        },
        "singleEvents": {
          "description": "Whether to expand recurring events into instances and only return single one-off events and instances of recurring events. Optional. The default is False.",
          "type": "boolean"
        },
        "syncToken": {
          "description": "Token from nextSyncToken to return only entries changed since the last list. Cannot be combined with iCalUID, orderBy, privateExtendedProperty, q, sharedExtende",
          "type": "string"
        },
        "timeMax": {
          "description": "Upper bound (exclusive) for an event's start time to filter by. Optional. If unset, no start-time upper bound is applied. Must be an RFC3339 timestamp with mand",
          "type": "string"
        },
        "timeMin": {
          "description": "Lower bound (exclusive) for an event's end time to filter by. Optional. If unset, no end-time lower bound is applied. Must be an RFC3339 timestamp with mandator",
          "type": "string"
        },
        "timeZone": {
          "description": "Time zone used in the response for formatting event times. Optional. Use an IANA time zone identifier (e.g., America/Los_Angeles). Defaults to the user's primar",
          "type": "string"
        },
        "updatedMin": {
          "description": "Lower bound for an event's last modification time (RFC3339). When specified, entries deleted since this time are always included regardless of showDeleted. Opti",
          "type": "string"
        }
      },
      "required": [],
      "toolkit": "googlecalendar",
      "version": "20260915_00"
    },
    "GOOGLECALENDAR_PATCH_EVENT": {
      "properties": {
        "anyone_can_add_self": {
          "description": "Whether anyone can invite themselves to the event (default: false).",
          "type": "boolean"
        },
        "attachments": {
          "description": "File attachments (max 25). Each with 'fileUrl' (required) and optional 'title', 'mimeType'. Requires supportsAttachments=true.",
          "type": "array"
        },
        "attendees": {
          "description": "List of valid email addresses for attendees (e.g., 'user@example.com'). Replaces existing attendees. Provide an empty list to remove all.",
          "type": "array"
        },
        "attendees_omitted": {
          "description": "Whether attendees may have been omitted from the event's representation.",
          "type": "boolean"
        },
        "calendar_id": {
          "description": "Identifier of the calendar. Use 'primary' for the primary calendar of the logged-in user. To find other calendar IDs, use the `calendarList.list` method. Must b",
          "type": "string"
        },
        "color_id": {
          "description": "Color ID for the event (1-11). Use GOOGLECALENDAR_COLORS_GET to retrieve available colors.",
          "type": "string"
        },
        "conference_data": {
          "description": "Input model for conference data.",
          "type": "object"
        },
        "conference_data_version": {
          "description": "API client's conference data support version. Set to 1 to manage conference details (e.g., Google Meet links); 0 (default) ignores conference data. Setting to 1",
          "type": "integer"
        },
        "description": {
          "description": "New description for the event; can include HTML.",
          "type": "string"
        },
        "end_time": {
          "description": "New end time (RFC3339 timestamp, e.g., '2024-07-01T11:00:00-07:00'). Uses `timezone` if provided, otherwise UTC. For all-day events, use YYYY-MM-DD format (excl",
          "type": "string"
        },
        "event_id": {
          "description": "The unique technical identifier of the event to update. IMPORTANT: This is NOT the event title/name. Event IDs are opaque strings typically base32hex encoded (5",
          "type": "string"
        },
        "extended_properties": {
          "description": "Input model for extended properties.",
          "type": "object"
        },
        "focus_time_properties": {
          "description": "Input model for focus time properties.",
          "type": "object"
        },
        "guests_can_invite_others": {
          "description": "Whether attendees other than the organizer can invite others (default: true).",
          "type": "boolean"
        },
        "guests_can_modify": {
          "description": "Whether attendees other than the organizer can modify the event (default: false).",
          "type": "boolean"
        },
        "guests_can_see_other_guests": {
          "description": "Whether attendees can see who the event's attendees are (default: true).",
          "type": "boolean"
        },
        "location": {
          "description": "New geographic location (physical address or virtual meeting link).",
          "type": "string"
        },
        "max_attendees": {
          "description": "Maximum attendees in response; does not affect invited count. If more, response includes organizer only. Must be positive.",
          "type": "integer"
        },
        "out_of_office_properties": {
          "description": "Input model for out-of-office properties.",
          "type": "object"
        },
        "recurrence": {
          "description": "RRULE, EXRULE, RDATE and EXDATE lines per RFC5545 for recurring events. Replaces existing recurrence rules if provided.",
          "type": "array"
        },
        "reminders": {
          "description": "Input model for reminders.",
          "type": "object"
        },
        "rsvp_response": {
          "description": "RSVP response status for the authenticated user. Updates only the current user's response status without affecting other attendees. Note: RSVP is only supported",
          "type": "string"
        },
        "send_updates": {
          "description": "Whether to send update notifications to attendees. Uses default user behavior if unspecified. Set explicitly to avoid unintended notifications when removing att",
          "type": "string"
        },
        "sequence": {
          "description": "Sequence number as per iCalendar specification. Incremented on each event update.",
          "type": "integer"
        },
        "source": {
          "description": "Input model for event source.",
          "type": "object"
        },
        "start_time": {
          "description": "New start time (RFC3339 timestamp, e.g., '2024-07-01T10:00:00-07:00'). Uses `timezone` if provided, otherwise UTC. For all-day events, use YYYY-MM-DD format. Wh",
          "type": "string"
        },
        "status": {
          "description": "Status of the event.",
          "type": "string"
        },
        "summary": {
          "description": "New title for the event.",
          "type": "string"
        },
        "supports_attachments": {
          "description": "Client application supports event attachments. Set to `True` if so.",
          "type": "boolean"
        },
        "timezone": {
          "description": "IANA Time Zone Database name for start/end times (e.g., 'America/Los_Angeles'). Used if `start_time` and `end_time` are provided and not all-day dates; defaults",
          "type": "string"
        },
        "transparency": {
          "description": "Whether the event blocks time on the calendar. 'opaque' (default) blocks time, 'transparent' does not.",
          "type": "string"
        },
        "visibility": {
          "description": "Visibility of the event.",
          "type": "string"
        },
        "working_location_properties": {
          "description": "Input model for working location properties.",
          "type": "object"
        }
      },
      "required": [
        "calendar_id",
        "event_id"
      ],
      "toolkit": "googlecalendar",
      "version": "20260915_00"
    }
  }
}
